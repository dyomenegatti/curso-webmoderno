let valor 
console.log(valor); // undefined

// console.log(valor2); - Variável não foi nem declarada, realmente não existe

valor = null; //null
console.log(valor);

const produto = {}
console.log(produto.preco); //Preço não está definido, então retorna undefined
console.log(produto);

produto.preco = 3.50;
console.log(produto);

